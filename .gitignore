# Dependencias
node_modules/

# Builds
backend/dist/
frontend/dist/
frontend/.angular/
coverage/

# Entornos locales (nunca subir credenciales — ni ANTHROPIC_API_KEY ni nada)
.env
.env.local
.env.deploy
*.local.env

# Sistema
.DS_Store
*.log

# Presentación de la sustentación y notas de trabajo — no son código ni entregable versionado
artifacts/*.pptx
artifacts/SLIDES.md
artifacts/RETO.md
artifacts/EMAIL.md

# Terraform (referencia de infraestructura, vive realmente en app-iac)
**/.terraform/
*.tfstate
*.tfstate.*
