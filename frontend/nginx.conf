server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA: cualquier ruta que no sea un archivo real cae al router de Angular.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
